# Security

Do not report suspected vulnerabilities in a public issue. Contact the Kiritsu
maintainer privately through the contact method published at the official
Kiritsu website.

Every installer URL used by a package manager must point to an immutable tagged
release and include the exact SHA-256 checksum of that asset. Release assets and
updater metadata must never be replaced after publication; publish a new version
instead.
